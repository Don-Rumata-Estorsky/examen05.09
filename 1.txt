
1)
    отличие глобальной и локальной переменных в том, что глобальные переменные вне опередённых функций, а локальные,
     наоборот живут только в функциях, классах и других. и не знают о существовании Глобальных переменных
    пример:


    def calculator_symma( first, second ):
        symma = first + second                    #local
        print(symma)

apple = 2                                       # global 
pear = 3

calculator_symma(apple, pear)





2)
    if работает как основное условие 
    elif работает в промежутке между if and else
    else работает только для основного условия if 

if 
elif
elif 
elif
else


3) None не может быть строкой работ числовых значений
None уже заданно таким образом что его можно найти
  в пустых же списках не возможно искать, будет выдовать ошибку




4)
    список это массив хронящий в себе : строки. цифры . True False
    insert( i , x )- встовляет элемент x на позицию i
    append( x   ) - добавляет элимент x в конец списка



5)
extend( new_list ) - расширяет список добавляя в конец списка все элементы из new_list
 "+" Добавляет в конец списка все элементы из new_list 
они дают одинаковый итог




6) цикл for прочёсывает все элементы в списке 

a=[1,5,7,9]
for el in a :
    print(el.enumerate())


